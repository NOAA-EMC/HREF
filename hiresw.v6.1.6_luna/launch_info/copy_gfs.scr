#! /bin/sh

module load prod_envir

cyc=${1}


# DATE=`cat /gpfs/hps/nco/ops/com/date/t${cyc}z | cut -c7-14`
DATE=`cat $COMROOTp1/date/t${cyc}z | cut -c7-14`

targdir=/gpfs/hps/ptmp/Matthew.Pyle/com/gfs/prod/gfs.${DATE}

echo $DATE $cyc

mkdir -p $targdir

cd $targdir

cp $COMROOTp1/gfs/prod/gfs.${DATE}/gfs.t${cyc}z.pgrb2.0p50.f0[0-4]? .

ls -l
