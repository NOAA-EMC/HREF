#! /bin/ksh

dir=${1}
core=${2}
cyc=${3}
dom=${4}

cd ${dir}

cp ../my* .

my* ${core} ${cyc}

cd ..

# ./copy_conuseast.scr_oldnames_single ${dir} ${core} ${cyc}

./copy_nonconus_awp.scr ${dir} ${core} ${cyc} ${dom}

# ./copy_wrfprsawc.scr ${dir} ${core} ${cyc}

./clean_dir.scr ${dir} ${core} ${cyc}
