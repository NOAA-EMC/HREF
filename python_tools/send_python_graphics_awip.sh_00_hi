#! /bin/ksh

#BSUB -oo send_graphics.out_pyth_00_hi
#BSUB -eo send_graphics.err_pyth_00_hi
#BSUB -P HRW-T2O
#BSUB -M 1500
#BSUB -J NSSL_GRAPHICS_TRANS
#BSUB -q "dev_transfer"
#BSUB -cwd /gpfs/hps3/emc/meso/noscrub/Matthew.Pyle/python_tools
#BSUB -n 1
#BSUB -W 0:52

cd /u/$USER    # cron does this for us - this is here just to be safe
. /etc/profile

if [ -a .profile ]; then
   . ./.profile
fi

if [ -a .bashrc ]; then
   . ./.bashrc
fi

base=/gpfs/hps3/emc/meso/noscrub/Matthew.Pyle/python_tools

cd /gpfs/hps3/ptmp/Matthew.Pyle/href_v2awip_python_00_hi

cyc=00

cp /gpfs/hps/nco/ops/com/date/t${cyc}z .

NDATE=/gpfs/hps/nco/ops/nwprod/prod_util.v1.0.12/exec/ndate

PDY=`cat t${cyc}z | cut -c7-14`

PDYCHECK=`cat T${cyc}Z | cut -c7-14`

if [ $PDY -ne $PDYCHECK ]
then
echo SENDING FOR DIFFERENT DATE THAN GRAPHICS CREATED
echo WILL QUIT
echo PDY was $PDY
echo PDYCHECK was $PDYCHECK

exit

fi


DAYDEL=`$NDATE -168 ${PDY}00 | cut -c1-8`

ssh mpyle@emcrzdm.ncep.noaa.gov "rm /home/www/emc/htdocs/mmb/mpyle/href_v2awips/${cyc}/*HIPROB.gif"
ssh mpyle@emcrzdm.ncep.noaa.gov "rm /home/www/emc/htdocs/mmb/mpyle/href_v2awips/${cyc}/*HIMEAN.gif"
ssh mpyle@emcrzdm.ncep.noaa.gov "rm /home/www/emc/htdocs/mmb/mpyle/href_v2awips/${cyc}/*HIPMMN.gif"


scp *.gif  mpyle@emcrzdm.ncep.noaa.gov:/home/www/emc/htdocs/mmb/mpyle/href_v2awips/${cyc}/

${base}/gen_info_ijk.scr_hrefv2awips $cyc

scp info.html mpyle@emcrzdm.ncep.noaa.gov:/home/www/emc/htdocs/mmb/mpyle/href_v2awips/${cyc}/

ssh mpyle@emcrzdm.ncep.noaa.gov "mkdir /home/www/emc/htdocs/mmb/mpyle/href_v2awips/${PDY}${cyc}"

scp *.gif info.html mpyle@emcrzdm.ncep.noaa.gov:/home/www/emc/htdocs/mmb/mpyle/href_v2awips/${PDY}${cyc}/

scp /gpfs/hps/nco/ops/com/date/t${cyc}z mpyle@emcrzdm.ncep.noaa.gov:/home/www/emc/htdocs/mmb/mpyle/href_v2awips/${cyc}/hi_t${cyc}z 

ssh mpyle@emcrzdm.ncep.noaa.gov "cp -R /home/www/emc/htdocs/mmb/mpyle/href_v2awips/raw_pieces/* /home/www/emc/htdocs/mmb/mpyle/href_v2awips/${PDY}${cyc}/"
ssh mpyle@emcrzdm.ncep.noaa.gov "cp /home/www/emc/htdocs/mmb/mpyle/href_v2awips/${cyc}/main*.php /home/www/emc/htdocs/mmb/mpyle/href_v2awips/${PDY}${cyc}/"

/gpfs/hps3/emc/meso/noscrub/Matthew.Pyle/python_tools/test.sh 00
