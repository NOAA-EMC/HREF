#BSUB -o /ptmp/Matthew.Pyle/nps_geogrid.log.%J
#BSUB -e /ptmp/Matthew.Pyle/nps_geogrid.log.%J
#BSUB -q "dev"
#BSUB -W 0:09
#BSUB -a poe
#BSUB -J GEOGRID
#BSUB -x
#BSUB -R "span[ptile=8]"
#BSUB -n 8
##
module load ics
module load ibmpe
##
#

PACKDIR=/meso/save/Matthew.Pyle/hiresw_vert/sorc/hiresw_nps.fd
WORKDIR=/stmp/Matthew.Pyle/nmmb_init

mkdir -p $WORKDIR

cd $WORKDIR
rm $WORKDIR/geo*.dio
rm $WORKDIR/*.grb

mkdir -p geogrid

cp $PACKDIR/NPS/geogrid/GEOGRID.TBL.NMB ./geogrid/GEOGRID.TBL
cp $PACKDIR/NPS/geogrid.exe .

cp $PACKDIR/namelist.nps .

# cp $PACKDIR/NPS/geogrid/testb.nml .
cp $PACKDIR/testb.nml .

ln -sf testb.nml fort.81

rm -rf cored*

mpirun.lsf ./geogrid.exe

cd $PACKDIR

# bsub < run_met.lsf
